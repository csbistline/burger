<div>
	<span class="font-weight-bold mt-2">{{burger_name}}</span><br>

	<button class="change-eaten btn btn-secondary btn-sm mb-4 mt-1" data-id="{{id}}" data-neweaten="{{devoured}}">
		{{#if devoured}}NOM NOM NOM{{else}}ORDER UP ANOTHER{{/if}}
	</button>

</div>
